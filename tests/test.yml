---

- name: Test PDM Mailserver Role
  hosts: all

  pre_tasks:
    - name: Update apt
      become: yes
      apt:
        cache_valid_time: 0
        update_cache: yes

  roles:
    - role: ansible-pdm-mailserver
      become: yes

